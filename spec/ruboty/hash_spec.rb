require "spec_helper"

describe Ruboty::Hash do
  it "has a version number" do
    expect(Ruboty::Hash::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
